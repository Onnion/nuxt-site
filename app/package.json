{
  "name": "qgdi",
  "version": "1.0.0",
  "description": "QGDI Site",
  "author": "Onnion",
  "private": true,
  "scripts": {
    "nuxt": "nuxt",
    "prestart": "npm i --unsafe-perm",
    "start": "nuxt",
    "build": "nuxt build",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "@nuxt/typescript-build": "^0.6.0",
    "css-loader": "^3.4.2",
    "node-sass": "^4.13.1",
    "sass-loader": "^8.0.2"
  }
}
